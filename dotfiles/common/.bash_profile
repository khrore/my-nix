[[ -f ~/.bashrc ]] && source ~/.bashrc
# uv
export PATH="$HOME/.local/bin:$PATH"

export PATH="/Users/khrore/.local/share/solana/install/active_release/bin:$PATH"

[ ! -f "$HOME/.x-cmd.root/X" ] || . "$HOME/.x-cmd.root/X" # boot up x-cmd.
